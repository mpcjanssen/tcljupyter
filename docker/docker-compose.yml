---
version: "2.1"
services:
  tcljupyter:
    user: 1000:1000
    container_name: tcljupyter
    image: mpcjanssen/tcljupyter:latest
    ports:
      - "127.0.0.1:7005:8888"
    volumes:
      - data:/data
      - config:/config
    restart: unless-stopped
    
volumes:
   data:
   config:
